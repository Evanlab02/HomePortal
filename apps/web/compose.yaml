services:
  web:
    container_name: hp-web
    build: .
    volumes:
      - ./conf:/etc/caddy/
      - hp-web-data:/data
    ports:
      - 443:443
    networks:
      - hp-network
    restart: unless-stopped
    logging:
      driver: gelf
      options:
        gelf-address: "udp://localhost:12201"
        tag: "{{.Name}}"
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 512M

volumes:
  hp-web-data:
    external: false
