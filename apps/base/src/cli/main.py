"""Main module for the CLI."""

import logging
from sys import exit

logger = logging.getLogger(__name__)


def start_cli(_: list[str]) -> None:
    """Start the CLI."""
    logger.info("Work in progress... Not ready yet.")
    exit(1)
